apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anouncy.fullname" . }}-test-connection"
  labels:
  {{- include "anouncy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "anouncy.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
